<div class="hero">
    <div class="hero__wrapper" {#data-component="Slider" data-animation="hero"#}>
        {# <div class="slider js-slider">
            {% for item in items %}
            <div class="slider__item js-item {% if loop.first %} is-active{% endif %}" data-item="{{loop.index0}}">
                <div class="slider__map js-map">
                    <img src="/media/map.png" alt="" class="slider__img">
                </div>
            </div>
            {% endfor %}
        </div>
        <div class="slider__elements" data-parallax="css-animation">
            <div class="slider__nav js-navigation">
                {% for item in items %}
                <div class="slider__point js-nav {% if loop.first %} is-active{% endif %}"><span></span></div>
                {% endfor %}
            </div>
            <div class="slider__captions">
                {% for item in items %}
                <div class="slider__caption js-caption" data-caption="{{loop.index0}}">
                    <span class="slider__sup js-el">{{item.sup}}</span>
                    <div class="slider__stat js-el css-animation">{{item.stat}}</div>
                    <div class="slider__sub js-el">
                        <div class="equal-icon"><img src="media/dotted-oval.png" alt="" class="css-animation"></div>
                        <span>{{item.sub}}</span>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div> #}
    </div>
</div>